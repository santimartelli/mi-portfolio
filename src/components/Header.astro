---

---

<header class="flex justify-between items-center mx-auto min-w-full border-b-zinc-700">
  <div class="h-full py-3">
    <a href="/" class="text-xl px-4 text-white italic">martelli.dev</a>
  </div>
  <nav id="navbar" class="flex flex-row gap-x-10 opacity-90 px-4 text-sm uppercase italic">
    <a href="/" class="nav-link">_Inicio</a>
    <a href="/about" class="nav-link">_Sobre Mí</a>
    <a href="/projects" class="nav-link">_Proyectos</a>
    <a href="/contact" class="nav-link">_Contacto</a>
  </nav>
</header>

<script>
document.addEventListener('DOMContentLoaded', function () {
  const currentPath = window.location.pathname;
  const navLinks = document.querySelectorAll('#navbar a');

  navLinks.forEach(link => {
    if (link.getAttribute('href') === currentPath) {
      link.classList.add('active');
    }
  });
});
</script>

<style>
.nav-link {
  position: relative;
  padding-right: 2px;
  color: #9ca3af; /* text-zinc-400 */
  background-image: linear-gradient(to right, white 0%, white 50%, #9ca3af 50%, #9ca3af 100%);
  background-size: 200% 100%;
  background-position: 100% 0;
  background-repeat: no-repeat;
  -webkit-background-clip: text;
  background-clip: text;
  color: transparent;
  transition: background-position 0.5s ease;
}

.nav-link:hover,
.nav-link:focus,
.nav-link.active {
  background-position: 0 0;
}

.nav-link.active {
  transition: none; /* Prevent animation on page load */
}


</style>