---
import "@fontsource/titillium-web";
import Layout from "../../layouts/Layout.astro";
import NavbarWithTranslations from "../../components/NavbarWithTranslations";
import ProjectsWithTranslations from "../../components/ProjectsWithTranslations";
import AboutMeWithTranslations from "../../components/AboutMeWithTranslations";
import HeroWithTranslations from "../../components/HeroWithTranslations";
import ContactWithTranslations from "../../components/ContactWithTranslations";
import FooterWithTranslations from "../../components/FooterWithTranslations";
import Divider from "../../components/Divider.astro";

// Cargar traducciones en inglés
const heroTranslations = await import("../../content/en/hero.json");
const navbarTranslations = await import("../../content/en/navbar.json");
const aboutmeTranslations = await import("../../content/en/aboutme.json");
const projectsTranslations = await import("../../content/en/projects.json");
const contactTranslations = await import("../../content/en/contact.json");
const footerTranslations = await import("../../content/en/footer.json");

const translations = {
  hero: heroTranslations.default,
  navbar: navbarTranslations.default,
  aboutme: aboutmeTranslations.default,
  projects: projectsTranslations.default,
  contact: contactTranslations.default,
  footer: footerTranslations.default
};
---

<Layout
  title="Santiago Martelli | Programmer and Web Developer"
  description="Hello! I'm Santiago Martelli, a programmer and web developer with knowledge of HTML, CSS, JavaScript, Vue.js, PHP, Laravel, SQL and more. Available for work!">
  <main>
    <!-- Navbar -->
    <NavbarWithTranslations translations={translations} client:load />

    <!-- Hero section with translations -->
    <section id="home" class="relative">
      <HeroWithTranslations translations={translations} client:visible />
    </section>

    <Divider />

    <!-- About me section -->
    <section id="about" class="relative">
      <AboutMeWithTranslations translations={translations} client:visible />
    </section>

    <Divider />

    <!-- Projects section -->
    <section id="projects" class="relative">
      <ProjectsWithTranslations translations={translations} client:visible />
    </section>

    <Divider />

    <!-- Contact section -->
    <section id="contact" class="relative">
      <ContactWithTranslations translations={translations} client:visible />
    </section>

    <!-- Footer -->
    <FooterWithTranslations translations={translations} client:visible />
  </main>
</Layout>